#include<stdio.h>
#include<iostream.h>
#include<conio.h>
#include<string.h>

 int main()
  {
   char str[20];
   int length;
   cout<<"Enter any string: ";
   gets(str);
   length = strlen(str);
   cout<<"String Length: "<<length;
   return 0;
}
